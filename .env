# 应用的基础路径（部署子路径时使用）
VITE_APP_BASE=/

# 静态资源加载模式：local（本地）或 remote（CDN）
VITE_ASSETS_MODE=local

# 当 VITE_ASSETS_MODE 为 remote 时，静态资源的 CDN 根地址
VITE_ASSETS_CDN=https://cdn.example.com

# 接口请求中用于携带 Token 的 Header 键名
VITE_API_TOKEN_KEY=Authorization

# 接口请求成功时的响应码
VITE_API_SUCCESS_CODE=200

# 是否启用接口 Mock 模式：1 启用，0 关闭
VITE_API_MOCK=1