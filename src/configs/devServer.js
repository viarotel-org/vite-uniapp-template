import {
  appBasePath,
  proxyPath,
  proxyPort,
  requestURL as proxyURL,
  requestFilePath,
  requestPath,
  useProxy,
} from './index'

export {
  useProxy,
  requestURL as proxyURL,
  proxyPath,
  proxyPort,
  appBasePath,
  requestPath,
  requestFilePath,
} from './index'

export default {
  useProxy,
  proxyURL,
  proxyPath,
  proxyPort,
  appBasePath,
  requestPath,
  requestFilePath,
}
