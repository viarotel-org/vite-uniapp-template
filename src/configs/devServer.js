import {
  useProxy,
  requestURL as proxyURL,
  proxyPath,
  proxyPort,
  appBasePath,
} from './index'

export {
  useProxy,
  requestURL as proxyURL,
  proxyPath,
  proxyPort,
  appBasePath,
} from './index'

export default {
  useProxy,
  proxyURL,
  proxyPath,
  proxyPort,
  appBasePath,
}
